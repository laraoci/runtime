# Pinned bats-core for CI. Generated by migrate-bats.sh.
# Pinned by SHA-256, not just version: a release asset can be substituted in
# place, so the hash — not the tag — is the trust anchor (see bin/fetch-tool.sh).
BATS_VERSION="1.14.0"
BATS_SHA256="bb537b70b15b732f6d8827dd6578e3d8ce166636ce1f18ea9a074184fcce9177"
BATS_URL="https://github.com/bats-core/bats-core/archive/refs/tags/v${BATS_VERSION}.tar.gz"
