version: 1
defaults:
  registry: ghcr.io/laraoci
  platforms: [linux/amd64]
php:
  "8.4": { status: supported, default: true }
size_budgets: { runtime: 1, orphan: 1 }
images:
  runtime: {}
  orphan: { parent: nonexistent }
