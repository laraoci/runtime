version: 1
defaults:
  registry: ghcr.io/laraoci
  platforms: [linux/amd64]
php:
  "8.4": { status: supported, default: true }
size_budgets: { alpha: 1, beta: 1 }
images:
  alpha: { parent: beta }
  beta: { parent: alpha }
